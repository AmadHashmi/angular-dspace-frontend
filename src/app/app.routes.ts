import { Routes } from '@angular/router';
import { CommunityListComponent } from './components/community-list/community-list.component';

export const routes: Routes = [{ path: '', component: CommunityListComponent }];
